Code/
│── index.html        	# 主页面
│── pages/           	# 其他HTML页面
   		└── artwork-detail.html	# 作品详情页
│── scripts/         	# JavaScript逻辑
   		└── index.js   		# 首页动态加载逻辑
   		└── artwork-detail.js  # 详情页动态加载逻辑
│── styles/          	# CSS样式
   		└── index.css 		# 首页样式
   		└── artwork-detail.css # 详情页样式
│── data/           	# 数据文件
   		└── artworks.json  	# 作品元数据（标题、作者、时长等）
   		└── artworks-content.json # 作品附加内容（歌词、备注等）
│── artwork/        	# 作品资源
   		└── audio/		# 音频文件（MP3格式）
   		└── score/		# 曲谱图片（JPG格式）
   		└── video/		# 视频文件（MP4格式）
   		└── cover/		# 视频封面（WEBP格式）
